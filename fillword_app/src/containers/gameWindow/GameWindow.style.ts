import styled from "styled-components";
import backgroundImg from './assets/v1016-b-09.jpg'

const Wrapper = styled.div`
  background: url(${backgroundImg});
  height: 100vh;
  overflow: hidden;
`;

const StyledGameWindow = {
  Wrapper,
};

export default StyledGameWindow;